#include<stdio.h>
#include <stdlib.h>
int main(){
	/*char *p;
	p=(char*)malloc(sizeof(char)*2);
	scanf("%2s",p);
	printf("%2s",p);
	free(p);*/
	char p[50];
	scanf("%s",p);
	printf("%s",p);
	return 0;
} 
